[project]
name = "internet-vibe-check"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sabelo", email = "transformmkhabela@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "google-api-python-client>=2.200.0",
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
internet-vibe-check = "internet_vibe_check:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
